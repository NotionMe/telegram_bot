# TikTok Telegram Bot

Цей Telegram бот створений для завантаження відео з TikTok та обробки їх у чатах. Написаний на TypeScript з використанням фреймворку [GramIO](https://gramio.dev/).

## 🌟 Можливості
- Завантаження відео за посиланнями `tiktok.com/` (через `ytdlp-nodejs`).
- Відправлення завантаженого відео в чат без водяних знаків (залежить від конфігурації `yt-dlp`).
- Вбудована система оцінювання відео (Like / Dislike) за допомогою Inline Keyboard.
- Збереження стану опрацьованих відео в оперативній пам'яті (MemoryService), що уникає дублювання завантажень відео, яке вже є в Telegram.
- Збереження голосів (лайків/дизлайків) у локальний файл `votes.json`.

## 🛠 Технологічний стек
- [Node.js](https://nodejs.org/)
- [TypeScript](https://www.typescriptlang.org/)
- [GramIO](https://gramio.dev/) - фреймворк для Telegram Bot API
- [ytdlp-nodejs](https://github.com/hiteshsingh28/ytdlp-nodejs) - для завантаження медіа
- [tsx](https://github.com/privatenumber/tsx) - для швидкого виконання TypeScript

## 📦 Встановлення та налаштування

1. **Клонуйте репозиторій** (якщо ви ще цього не зробили).

2. **Встановіть залежності**:
   ```bash
   npm install
   ```

3. **Створіть файл конфігурації `.env`** у корені проекту та додайте свій токен:
   ```env
   BOT_TOKEN=ваш_токен_від_botfather
   ```

## 🚀 Запуск бота

### Режим розробки (з автоперезавантаженням)
Для запуску в режимі розробки, який буде автоматично перезапускати бота при зміні файлів:
```bash
npm run dev
```

### Режим продакшн
Для запуску у продакшн середовищі:
```bash
npm run start
```

## 📂 Структура проекту
- `src/handlers` — обробники подій (команди, повідомлення, callback-кнопки).
- `src/services` — бізнес-логіка (завантаження відео, система голосування, робота з пам'яттю).
- `src/repository` — шар доступу до даних (в оперативній пам'яті або файлах).
- `src/model` — моделі даних.
- `src/shared` — спільні утиліти (клавіатури, callback дані).
- `src/di` — налаштування Dependency Injection контейнера.

## 📝 Нотатки
- **Пам'ять завантажень**: Бот зберігає `file_id` завантажених відео в оперативній пам'яті, щоб швидко відправляти їх повторно при тому ж самому посиланні в тому ж самому чаті, не завантажуючи їх знову з TikTok. Дані очищаються при перезапуску бота.
- **Голосування**: Голоси зберігаються локально у файлі `votes.json`.
- Для завантаження відео в системі повинен бути встановлений Python та `yt-dlp` (зазвичай встановлюється автоматично залежністю `ytdlp-nodejs` або потребує системного `yt-dlp`).